%section.invite.container
  .row
    .col-sm-10.col-sm-offset-1
      = bootstrap_form_for @invitation, class: 'invite' do |f|
        %header
          %h1 Invite a friend to join MyFlix!
        %hr
        %fieldset
        = f.email_field :recipients_email, label: "Friend's Email"
        = f.text_field :recipients_name, label: "Friend's Name"
        = f.text_area :message, label: 'Your Message', placeholder: "If you don't specify a message your Invitation will say this: 'MyFlix is an awesome video on demand service, with a selection that's unmatched. Join Today!'", rows: 6
        = f.submit 'Send Invitation', class: 'btn btn-primary'
